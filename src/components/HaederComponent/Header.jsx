
import style from './Header.module.css';

function header() {
    return(
        <header>
            <h1 className={style.title}>IL MIO BLOG</h1>
        </header>
    )
};

export default header;

